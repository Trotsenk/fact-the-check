# Зависимости проекта. Установка: python -m pip install -r requirements.txt
anthropic>=1.7,<2      # официальная библиотека Claude
httpx>=0.28,<1         # запросы к PubMed, Europe PMC, ClinicalTrials.gov
python-dotenv>=1.2,<2  # чтение ключа из .env
ipykernel>=7.3,<8      # окружение проекта как ядро Jupyter
